import * as grpc from "grpc";

import * as <%= protoName %> from "./<%= protoName %>.generated";

export interface <%= serviceName %>_Interface extends grpc.Client {
<% for (const method of methods) { %>
    <%= method.name %>(request: <%= `${protoName}.I${method.requestType}` %>, callback?: (error: Error, response: <%= `${protoName}.I${method.responseType}` %>) => void);
<% } %>
}